# Plan {{功能名}} — {{简述}}

> 状态：{{规划中 / 实施中 / review 中 / review 打回 / 已完成 / 已废弃}}
> 日期：{{YYYY-MM-DD}}

---

## 零、Skill 调用要求

> Skill **只在计划实施阶段加载**，规划阶段不加载。批注完成、收到「执行」指令后必须加载以下 Skill。

| Skill | 必须性 | 调用时机 | 用途 |
|-------|--------|----------|------|
| `.opencode/skills/project-standards/SKILL.md` | **必须** | 实施前（仅一次） | 项目结构、注释规约、MARK 分段、SwiftLint 规则、UI 布局规范 |
| `.opencode/skills/naming-conventions/SKILL.md` | **必须** | 实施前（仅一次） | 类/协议/常量命名、命名空间、资源命名 |
| `.opencode/skills/urouter/SKILL.md` | 按需 | 涉及路由变更时 | 路由注册、跳转规范 |

> **重要：实施前必须加载 Skill**，确保注释、命名规范落实到每一行代码

**规划阶段自校验**（规划完成后逐项确认，不强制加载 Skill）：

- [ ] 新建文件目录符合标准模块结构
- [ ] 类/协议命名采用 UpperCamelCase，属性/方法采用 lowerCamelCase
- [ ] 类文件按 subviews → 属性 → LifeCycle → 方法 → DataSource/Delegate 顺序组织
- [ ] 新增代码必须添加文档注释
- [ ] 涉及路由时遵循现有注册模式

**实施前检查清单**（开始写代码前必须确认）：

- [ ] 已加载 project-standards skill
- [ ] 已加载 naming-conventions skill
- [ ] 确认新增类必须加 `///` 文档注释
- [ ] 确认新增公开方法必须加完整文档注释（参数+返回值）
- [ ] 确认修改代码同步更新对应注释

---

## 一、需求背景

### 1.1 背景

{{说明为什么要做这个功能，解决什么痛点}}

### 1.2 功能需求

| # | 需求项 | 优先级 | 说明 |
|---|--------|--------|------|
| 1 | {{需求}} | P0/P1/P2 | {{说明}} |

### 1.3 非功能需求（如适用）

| # | 需求项 | 说明 |
|---|--------|------|
| {{编号}} | {{性能/安全/兼容性等}} | {{说明}} |

---

## 二、技术方案

### 2.1 整体架构

```
{{架构图：模块关系、数据流向}}
```

### 2.2 目录结构

```
YouZhiYuan/Module/{{模块名}}/
├── Model/
│   └── {{模型名}}Model.swift
├── ViewModel/（如适用）
│   └── {{功能名}}ViewModel.swift
├── View/
│   └── {{页面名}}ViewController.swift
└── Service/（如适用）
    └── {{服务名}}Service.swift
```

### 2.3 数据模型

```swift
// YouZhiYuan/Module/{{模块名}}/Model/{{模型名}}Model.swift
struct {{ModelName}} {
    {{属性定义}}
}
```

**API 接口（如适用）**：

| 接口 | 方法 | 说明 | 请求模型 | 响应模型 |
|------|------|------|----------|----------|
| `{{path}}` | GET/POST | {{说明}} | {{Request}} | {{Response}} |

### 2.4 核心流程

**1. {{主流程名称}}**：
```
{{步骤伪代码}}
```

**2. {{异常/边界流程}}**：
```
{{处理方式}}
```

### 2.5 关键设计决策（如适用）

| # | 决策 | 结论 | 原因 |
|---|------|------|------|
| {{编号}} | {{决策}} | {{结论}} | {{原因}} |

---

## 三、UI 设计（如适用）

### 3.1 页面结构

| 页面 | 文件 | 说明 |
|------|------|------|
| {{页面名}} | `{{路径}}` | {{说明}} |

### 3.2 交互说明

| # | 交互 | 触发 | 响应 |
|---|------|------|------|
| {{编号}} | {{交互}} | {{触发方式}} | {{响应行为}} |

---

## 四、实现步骤

### Step 1: {{步骤名称}}

**新建/修改文件**: `YouZhiYuan/Module/{{路径}}`

> {{步骤说明}}

- {{具体动作}}

**验证**: 编译通过

### Step 2 ~ Step N: {{按需增减}}

{{同上格式}}

---

## 五、不修改的内容

| 文件/模块 | 原因 |
|-----------|------|
| `{{路径}}` | {{原因}} |

---

## 六、验证要点

### 功能验证

| # | 验证项 | 关注点 |
|---|--------|--------|
| 1 | {{验证项}} | {{关注点}} |

### 回归验证

| # | 验证项 |
|---|--------|
| 1 | SwiftLint 检查无新增 warning |
| 2 | Xcode Build (YouZhiYuan-Develop) 无编译错误 |
| 3 | 所有新增类/方法/属性已添加 `///` 文档注释 |
| 4 | 注释与代码逻辑一致，无误导性内容 |
| 5 | {{其他回归项}} |

---

## 七、文件变更清单

| 操作 | 文件 | 行数估算 |
|------|------|----------|
| 新建 | `{{路径}}` | ~{{行数}}行 |
| 修改 | `{{路径}}` | +{{行数}}行 |

---

## 八、阶段跟踪

| 阶段 | 状态 | 备注 |
|------|------|------|
| Step 1: {{步骤名}} | ⬜ | {{备注}} |
| 编译验证 | ⬜ | |
