# Plan {{功能名}} — {{简述}}

> 状态：{{规划中 / 实施中 / review 中 / review 打回 / 已完成 / 已废弃}}
> 日期：{{YYYY-MM-DD}}

---

## 零、Skill 调用要求

> Skill **只在计划实施阶段加载**，规划阶段不加载。批注完成、收到「执行」指令后必须加载以下 Skill。

| Skill | 必须性 | 调用时机 | 用途 |
|-------|--------|----------|------|
| `.opencode/skills/project-standards/SKILL.md` | **必须** | 实施前（仅一次） | 项目结构、注释规约、MARK 分段、SwiftLint 规则、UI 布局规范 |
| `.opencode/skills/naming-conventions/SKILL.md` | **必须** | 实施前（仅一次） | 类/协议/常量命名、命名空间、资源命名 |
| `.opencode/skills/urouter/SKILL.md` | 按需 | 涉及路由变更时 | 路由注册、跳转规范 |

> **重要：实施前必须加载 Skill**，确保注释、命名规范落实到每一行代码

**规划阶段自校验**（规划完成后逐项确认，不强制加载 Skill）：

- [ ] 新建文件目录符合标准模块结构
- [ ] 类/协议命名采用 UpperCamelCase，属性/方法采用 lowerCamelCase
- [ ] 类文件按 subviews → 属性 → LifeCycle → 方法 → DataSource/Delegate 顺序组织
- [ ] 新增代码必须添加文档注释
- [ ] 重构时原有注释不得删除，仅可追加纠正注释
- [ ] 涉及路由时遵循现有注册模式

**实施前检查清单**（开始写代码前必须确认）：

- [ ] 已加载 project-standards skill
- [ ] 已加载 naming-conventions skill
- [ ] 确认新增类必须加 `///` 文档注释
- [ ] 确认新增公开方法必须加完整文档注释（参数+返回值）
- [ ] 确认修改代码同步更新对应注释
- [ ] 确认重构时原有注释不得删除或篡改

---

## 一、需求背景

### 1.1 现状

{{描述当前实现的问题：文件行数、职责耦合、缺失功能、待规范项等}}

| 系统/模块 | 文件 | 行数 | 职责 | 问题 |
|-----------|------|------|------|------|
| {{模块1}} | `{{路径}}` | {{行数}} | {{职责}} | {{问题}} |
| {{模块2}} | `{{路径}}` | {{行数}} | {{职责}} | {{问题}} |

### 1.2 必须解决的问题

| # | 问题 | 优先级 | 说明 |
|---|------|--------|------|
| 1 | {{问题}} | P0/P1/P2 | {{具体说明}} |

### 1.3 暂不解决

| # | 问题 | 原因 |
|---|------|------|
| {{编号}} | {{问题}} | {{原因}} |

---

## 二、技术方案

### 2.1 架构总览

```
重构前（当前）：
{{现有架构树}}

重构后：
{{目标架构树，每个文件标注预估行数和职责}}
```

**设计原则**：
- 单一职责：{{说明}}
- 依赖倒置：{{说明}}
- 渐进式：{{说明每步可编译}}

### 2.2 {{组件1名称}} — {{职责}}（从 {{源文件}} 提取 / 新增）

```swift
protocol {{DelegateProtocolName}}: AnyObject {
    {{方法签名}}
}

final class {{ClassName}} {
    weak var delegate: {{DelegateProtocolName}}?

    // MARK: - {{功能分组}}
    {{公开接口}}
}
```

**内部实现要点**：
- {{要点1}}
- {{要点2}}
- **线程安全**：{{说明哪些方法在哪个线程调用，如何保护}}

### 2.3 {{组件2名称}} — {{职责}}

{{同上格式}}

### 2.4 {{组件N名称}} — {{职责}}

{{同上格式，按需增减}}

### 2.5 核心流程

**1. {{流程名称1}}（{{场景}}）**：
```
{{流程步骤伪代码，用缩进和箭头表示调用链}}
```

**2. {{流程名称2}}（{{场景}}）**：
```
{{流程步骤}}
```

**3. {{流程名称3}}（{{场景}}）**：
```
{{流程步骤，如有行为变更需标注}}
```

**4. {{竞态/边界处理}}**：
- {{处理方式1}}
- {{处理方式2}}

---

## 三、实现步骤

### Step 1: {{步骤名称}}

**修改/新建文件**: `YouZhiYuan/Module/{{路径}}`

> {{步骤说明：为什么这步在前}}

- {{具体动作1}}
- {{具体动作2}}

**验证**: 编译通过

### Step 2: {{步骤名称}}

**新建文件**: `YouZhiYuan/Module/{{路径}}`

- {{具体动作1}}
- {{具体动作2}}

**验证**: 新文件编译通过（原文件暂不动）

### Step 3 ~ Step N: {{按需增减}}

{{同上格式}}

> **如某步骤工作量大**，按子任务顺序逐步实现，每个子任务完成后可本地提交（不推远程）：
> 1. {{子任务1}}
> 2. {{子任务2}}

---

## 四、不修改的内容

| 文件/模块 | 原因 |
|-----------|------|
| `{{路径}}` | {{原因}} |

---

## 五、验证要点

### 功能验证

| # | 验证项 | 关注点 |
|---|--------|--------|
| 1 | {{验证项}} | {{关注点}} |

### 行为变更（如有）

| # | 场景 | 原行为 | 新行为 | 风险 |
|---|------|--------|--------|------|
| {{编号}} | {{场景}} | {{原行为}} | {{新行为}} | {{风险/注意事项}} |

### 回归验证

| # | 验证项 |
|---|--------|
| 1 | SwiftLint 检查无新增 warning |
| 2 | Xcode Build (YouZhiYuan-Develop) 无编译错误 |
| 3 | 所有新增类/方法/属性已添加 `///` 文档注释 |
| 4 | 原有注释未删除或篡改，仅追加纠正注释 |
| 5 | 注释与代码逻辑一致，无误导性内容 |
| 6 | {{其他回归项}} |

---

## 六、文件变更清单

| 操作 | 文件 | 行数估算 |
|------|------|----------|
| 新建 | `{{路径}}` | ~{{行数}}行 |
| 重构 | `{{路径}}` | ~{{行数}}行（原{{行数}}行） |
| 修改 | `{{路径}}` | +{{行数}}行 |

**本次不修改**：
- `{{路径}}`（{{原因}}，下次 plan）

---

## 七、阶段跟踪

| 阶段 | 状态 | 备注 |
|------|------|------|
| Step 1: {{步骤名}} | ⬜ | {{备注}} |
| Step 2: {{步骤名}} | ⬜ | {{备注}} |
| 编译验证 | ⬜ | |
