# Code Review {{版本号}} - {{功能标题}}

> **关联 Plan：** [plan-{{功能名}}-v{{版本号}}.md](../Plans/plan-{{功能名}}-v{{版本号}}.md)
> **关联 Review：** [review-{{功能名}}-v{{版本号}}.md](../Reviews/review-{{功能名}}-v{{版本号}}.md)
> **规范依据：** `.opencode/skills/coding-standards/SKILL.md`、`.opencode/skills/naming-conventions/SKILL.md`
> **Reviewer：** {{reviewer 姓名 / AI}}
> **Review 日期：** {{YYYY-MM-DD}}

---

## 一、审查范围

> 从 plan 的文件清单导入，只审查本次变更涉及的文件。

### 新增文件

| 文件路径 | 类型 | 审查结果 |
|---------|------|---------|
| `{{path}}` | ViewController / View / ViewModel / Model / Service / Extension / Utility | ✅ / ⚠️ 有问题 |

### 修改文件

| 文件路径 | 改动说明 | 审查结果 |
|---------|---------|---------|
| `{{path}}` | {{改动说明}} | ✅ / ⚠️ 有问题 |

---

## 二、命名规范检查

> 依据：`.opencode/skills/naming-conventions/SKILL.md`

| # | 检查项 | 状态 | 问题文件（若有） |
|---|--------|------|----------------|
| 2.1 | 类型/协议使用 UpperCamelCase | ✅ / ❌ / N/A | |
| 2.2 | 方法/属性使用 lowerCamelCase | ✅ / ❌ / N/A | |
| 2.3 | 协议描述能力用 `-able` / `-ible` 后缀，delegate 用 `Delegate` 后缀 | ✅ / ❌ / N/A | |
| 2.4 | 常量 lowerCamelCase + 类型命名空间（`App.user`、`Host.api`） | ✅ / ❌ / N/A | |
| 2.5 | 命名空间用嵌套 enum，避免多模块类型冲突 | ✅ / ❌ / N/A | |
| 2.6 | 类名/属性名省略冗余前缀（上下文已明确时） | ✅ / ❌ / N/A | |
| 2.7 | 无拼音混写、无不规范缩写（`ucode`/`youzy`/`tzy` 除外） | ✅ / ❌ / N/A | |
| 2.8 | 非暴露属性/方法加 `private` / `fileprivate` | ✅ / ❌ / N/A | |
| 2.9 | 图片资源命名：`模块_业务_控件_状态` | ✅ / ❌ / N/A | |

---

## 三、项目结构检查

> 依据：`.opencode/skills/coding-standards/SKILL.md` §1

| # | 检查项 | 状态 | 问题文件（若有） |
|---|--------|------|----------------|
| 3.1 | 文件放置在正确的模块目录下 | ✅ / ❌ / N/A | |
| 3.2 | ViewController 不包含业务逻辑，业务下沉到 Service/ViewModel | ✅ / ❌ / N/A | |
| 3.3 | Model/Entity 仅包含数据定义，不包含网络请求逻辑 | ✅ / ❌ / N/A | |
| 3.4 | Extension 按功能分文件，不堆砌在同一个文件中 | ✅ / ❌ / N/A | |
| 3.5 | 桥接头文件 `YouZhiYuan-Bridging-Header.h` 按需更新（涉及 ObjC 混编时） | ✅ / ❌ / N/A | |

---

## 四、编码规范检查

> 依据：`.opencode/skills/coding-standards/SKILL.md` §2

| # | 检查项 | 状态 | 问题文件（若有） |
|---|--------|------|----------------|
| 4.1 | 行长度不超过 900，函数体不超过 100/200 行，类型体不超过 300/400 行 | ✅ / ❌ / N/A | |
| 4.2 | 使用 `guard` 提前返回，减少嵌套层级 | ✅ / ❌ / N/A | |
| 4.3 | 可选值安全处理：`if let` / `guard let` / `??`，禁止 `!` 强制解包 | ✅ / ❌ / N/A | |
| 4.4 | 闭包中使用 `[weak self]` 避免循环引用 | ✅ / ❌ / N/A | |
| 4.5 | 优先使用 `let`，判空用 `isEmpty`，判 nil 用 `!= nil` | ✅ / ❌ / N/A | |
| 4.6 | 避免使用 `self`（仅编译器要求时使用） | ✅ / ❌ / N/A | |
| 4.7 | 空大括号写成 `{}`，数据模型属性指明类型 | ✅ / ❌ / N/A | |
| 4.8 | 不使用 `print`，禁止注释代码 | ✅ / ❌ / N/A | |

---

## 五、注释规范检查

> 依据：`.opencode/skills/coding-standards/SKILL.md` §1

| # | 检查项 | 状态 | 问题文件（若有） |
|---|--------|------|----------------|
| 5.1 | `///` 文档注释用于类/属性/方法声明，`//` 用于行内注释 | ✅ / ❌ / N/A | |
| 5.2 | `//` 和 `///` 后保留一个空格 | ✅ / ❌ / N/A | |
| 5.3 | 中文注释优先，专有名词保持英文 | ✅ / ❌ / N/A | |
| 5.4 | MARK 分段按固定顺序（subviews → public → private → LifeCycle → Events → Public → Private → DataSource → Delegate） | ✅ / ❌ / N/A | |
| 5.5 | 所有枚举必须注释 | ✅ / ❌ / N/A | |
| 5.6 | 无注释掉的废弃代码，原有注释不得删除 | ✅ / ❌ / N/A | |

---

## 六、UI 布局检查

> 依据：`.opencode/skills/coding-standards/SKILL.md` §3

| # | 检查项 | 状态 | 问题文件（若有） |
|---|--------|------|----------------|
| 6.1 | 优先使用 xib 创建 UI | ✅ / ❌ / N/A | |
| 6.2 | 复杂布局使用 UIStackView | ✅ / ❌ / N/A | |
| 6.3 | 使用 Autolayout 布局，非必要不使用 frame | ✅ / ❌ / N/A | |

---

## 七、ObjC/Swift 混编检查

> 仅当本次变更涉及混编时填写，否则填 N/A。

| # | 检查项 | 状态 | 问题文件（若有） |
|---|--------|------|----------------|
| 7.1 | Swift 调用 ObjC：桥接头文件已引入对应 `.h` | ✅ / ❌ / N/A | |
| 7.2 | ObjC 调用 Swift：`@objc` 标记正确，`-Swift.h` 导入正常 | ✅ / ❌ / N/A | |
| 7.3 | 可选值桥接：ObjC `nullable` / `nonnull` 标注正确 | ✅ / ❌ / N/A | |
| 7.4 | 文件语言已在 plan 中标注 | ✅ / ❌ / N/A | |

---

## 八、问题汇总

> 检查项全部通过则填「无」。

| # | 文件路径 | 行号 | 问题描述 | 严重程度 | 修复建议 | 状态 |
|---|---------|------|---------|---------|---------|------|
| 1 | `{{path}}` | {{行号}} | {{问题}} | 🔴 必须修 / 🟡 建议修 / 🔵 风格建议 | {{建议}} | ⬜ / ✅ |

**严重程度说明：**

| 级别 | 含义 | 能否通过 |
|------|------|---------|
| 🔴 必须修 | 违反强制规范，影响可读性或正确性 | 修完才能通过 |
| 🟡 建议修 | 违反推荐规范，本次最好修 | 可有条件通过 |
| 🔵 风格建议 | 可以更好，但不强制 | 可通过，记录即可 |

---

## 九、签收

**签收前置条件：**

- [ ] 所有 🔴 必须修 问题已修复
- [ ] 🟡 建议修 问题已修复或注明原因
- [ ] SwiftLint 检查通过：`swiftlint lint --path YouZhiYuan`
- [ ] 项目编译无错误

**审查结论：** `通过` / `有条件通过（🟡 遗留已注明）` / `不通过（🔴 未修复）`

**签收日期：** {{YYYY-MM-DD}}

---

> 发现的问题若需回 plan 修复，请同步更新 plan 阶段跟踪表：
> 阶段状态改为 `⚠️ review 打回`，备注填写本文件名 + 问题编号（如 `code-review-{功能名}-v{版本号} #1`）。
